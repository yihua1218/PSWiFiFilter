<Window x:Class="WiFiFilterGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WiFiFilterGUI"
        mc:Ignorable="d"
        Title="PSWiFiFilter GUI" Height="500" Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Text="Wi-Fi SSID Filter Manager" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Available Networks -->
        <GroupBox Grid.Row="1" Grid.Column="0" Header="Available Networks">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ListView ItemsSource="{Binding AvailableNetworks}" SelectedItem="{Binding SelectedAvailableNetwork}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="SSID" DisplayMemberBinding="{Binding SSID}" Width="180"/>
                            <GridViewColumn Header="Signal" DisplayMemberBinding="{Binding Signal}" Width="60"/>
                            <GridViewColumn Header="Security" DisplayMemberBinding="{Binding Security}" Width="100"/>
                        </GridView>
                    </ListView.View>
                </ListView>
                <Button Grid.Row="1" Content="Scan Networks" Command="{Binding ScanCommand}" Margin="0,5,0,0"/>
            </Grid>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="10">
            <Button Content="Add &gt;&gt;" Command="{Binding AllowCommand}" Margin="0,0,0,10" Padding="10,5"/>
            <Button Content="&lt;&lt; Remove" Command="{Binding RemoveCommand}" Padding="10,5"/>
        </StackPanel>

        <!-- Allowed Networks -->
        <GroupBox Grid.Row="1" Grid.Column="2" Header="Allowed SSIDs (Whitelist)">
            <ListView ItemsSource="{Binding AllowedNetworks}" SelectedItem="{Binding SelectedAllowedNetwork}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="SSID" DisplayMemberBinding="{Binding SSID}" Width="200"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <!-- Main Actions -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Clear All Filters (Show All)" Command="{Binding ClearFiltersCommand}" Margin="0,0,10,0" Padding="10,5" Background="#FFDDDD"/>
            <Button Content="Apply Filters" Command="{Binding ApplyFiltersCommand}" Padding="20,5" FontWeight="Bold"/>
        </StackPanel>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" Grid.ColumnSpan="3" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <ProgressBar Width="100" Height="15" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
